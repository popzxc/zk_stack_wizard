use crate::runner::InitContext;
use dialoguer::Input;

fn init_context() -> InitContext {
    todo!()
    // let hyperchain_name = Input::new().with_prompt("Please enter the name of your hyperchain")
}
